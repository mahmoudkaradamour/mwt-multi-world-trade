steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'us-central1-docker.pkg.dev/$PROJECT_ID/mwt-repo/mwt-core', './backend']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'us-central1-docker.pkg.dev/$PROJECT_ID/mwt-repo/mwt-core']

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - run
      - deploy
      - mwt-core
      - --image=us-central1-docker.pkg.dev/$PROJECT_ID/mwt-repo/mwt-core
      - --region=us-central1
      - --platform=managed
      - --allow-unauthenticated
